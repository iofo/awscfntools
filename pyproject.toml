[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
requires-python = ">= 3.12"
name = "awscfntools"
version = "0.1.0"
dependencies = [
        "boto3",
        "ec2-metadata"]
